using Fairwood.Math;
using UnityEngine;

/// <summary>
/// 摄像机移动
/// </summary>
public class CameraPan : MonoBehaviour
{
    //public Vector3 Offset;

    //public float FollowSpeed = 0.1f;
    //void Awake()
    //{
    //    Offset = transform.position;
    //}

    //void Update()
    //{
    //    if (MainController.Instance.FocusedUnit != null)
    //        transform.position = Vector3.Lerp(transform.position, MainController.Instance.FocusedUnit.transform.position.SetV3Y(0) + Offset, FollowSpeed);
    //}
}